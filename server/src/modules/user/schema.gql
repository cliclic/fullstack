scalar DateTime

enum Role {
    User
    Admin
}

type AccessToken {
    token: String!
    createdAt: DateTime!
}

type User {
    _id: ID!
    login: String!
    username: String!
    displayName: String!
    roles: [Role!]!
    createdAt: DateTime!
    updatedAt: DateTime!
}

type Query {
    me: User!,
    users: [User]!
}

type Mutation {
    
}
